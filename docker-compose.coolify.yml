version: '3.8'

services:
  rhwp-studio:
    build:
      context: .
      dockerfile: rhwp-studio/Dockerfile.coolify
    container_name: rhwp-studio
    ports:
      - "7700:7700"
    restart: always
    environment:
      - NODE_ENV=development
